name: Fifth workflow
run-name: first run 
on: 
  push:
    branches:
      -  main
  workflow_dispatch:

jobs:
  First_job:
    runs-on: ubuntu-latest
    steps:
      -  name: First Step
      -  run: echo "THis is the example of WORKFLOW DISPATCH"
      
